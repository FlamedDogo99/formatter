{
  "name": "editor",
  "version": "1.0.0",
  "description": "Document editor — builds to a single self-contained HTML file",
  "scripts": {
    "build": "node build-single-file.mjs"
  },
  "type": "module",
  "dependencies": {
    "fast-json-patch": "^3.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}